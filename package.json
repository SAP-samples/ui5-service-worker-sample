{
  "name": "ui5-service-worker-sample",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build-ui5": "cd openui5-sample-app && npm run build",
    "build-h2": "cd openui5-sample-app && npm run build --h2",
    "build-service-worker": "cd ui5-service-worker && npm run setup",
    "start": "cd openui5-sample-app && npm run serve-dist -- --port 8080 --o index.html",
    "start-dist-self-contained": "cd openui5-sample-app && npm run build-self-contained && npm run serve-dist -- --port 8080"
  },
  "workspaces": [
    "openui5-sample-app",
    "ui5-service-worker"
  ]
}
